---
description: 校验某模块的当前实现是否符合其规约的 §3 接口契约与 §5 不变式，报告偏差
---

# /spec-check <module>

参数 `<module>` 为模块名（如 `kernel`、`provider-pool`）。

1. 读 `docs/spec/modules/<module>.md`，提取 §3 关键接口/契约 与 §5 不变式。
2. 读该模块当前实现代码。
3. 逐条比对：公开接口的语义是否一致、每条不变式是否被实现与测试保证。
4. 输出偏差清单（符合 / 偏离 / 缺失），偏离项给出具体位置与建议。不自动改代码，只报告。
