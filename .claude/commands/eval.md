---
description: 在数据集上端到端评测，报告逐题对错与汇总结果
---

# /eval

前提：[[eval]] 与 [[pipeline]] 模块实现已就绪。

1. 调用 eval 模块的 `run_eval` 在 GAIA 文本子集等数据集上跑全系统。
2. 汇总逐题对错与结果。
3. 输出逐题对错与汇总，便于回归对比。
4. 评测是为了看系统能不能跑出结果，不是为了凑某个准确率/成本数字。
