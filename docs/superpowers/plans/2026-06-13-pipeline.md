# Pipeline Implementation Plan

> Superpowers skills were not available in this Codex session, so this records the same writing-plans/TDD/verification loop for the `pipeline` module.

**Goal:** Build the deterministic Plan→Search→Verify→Write research script so an open question becomes a cited report, search runs in parallel, and only verified citations can enter the final write step.

**Architecture:** Create a `deep_research.pipeline` package. `models.py` owns Plan, SearchResult, VerifiedClaim, ReportDraft, and Report. `research.py` owns `ResearchPipeline`, depending on `Kernel`, MCP search/fetch tools, and verifier. Tests use real kernel primitives with fake subagent/tools to verify end-to-end report generation, parallel-vs-serial search latency, and filtering of unsupported citations before Write.

**Tech Stack:** Python 3.14, kernel agent/pipeline/parallel, mcp-tools, verifier, pydantic, pytest, pyright strict, ruff.

## File Structure

- Create `src/deep_research/pipeline/models.py`: Plan/Search/Verify/Report pydantic models.
- Create `src/deep_research/pipeline/research.py`: `ResearchPipeline` script and dependency protocols.
- Create `src/deep_research/pipeline/__init__.py`: public exports.
- Create `tests/pipeline/test_pipeline.py`: acceptance tests for §5 invariants and §8 behavior.
- Modify `docs/guide/01-build-order.md`: mark `pipeline` cells complete after T1 gate passes.

## Acceptance Mapping

- End-to-end research returns report with verified citations: `tests/pipeline/test_pipeline.py::test_research_returns_report_with_verified_citations`.
- Parallel search is faster than serial baseline: `test_parallel_search_is_faster_than_serial_baseline`.
- Unsupported citation does not enter Write or report: `test_unverified_citation_does_not_enter_write_or_report`.

## Tasks

- [x] Write pipeline acceptance tests from `docs/spec/modules/pipeline.md` §8.
- [x] Implement pydantic models for all four stages.
- [x] Implement Plan with `kernel.agent`.
- [x] Implement Search with `kernel.pipeline` and `kernel.parallel`.
- [x] Implement Verify handoff and Write using only verified claims.
- [x] Run T1 quality gate: full tests, pyright strict, ruff lint, ruff format, and defensive try review.
